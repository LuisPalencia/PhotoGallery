package com.cice.photogallery.constants

object Constants {
    // Constans for pemission requests
    const val READ_EXTERNAL_STORAGE_PERMISSIONS_CODE = 100
    const val WRITE_EXTERNAL_STORAGE_PERMISSIONS_CODE = 101
    const val CAMERA_PERMISSIONS_CODE = 102

    // Constant for the imagePosition argument passed to the ImageSliderFramgnet
    const val IMAGE_POSITION = "IMAGE_POSITION"
}